// Object literal is the base form that we create a normal object

// for example

const circle = {
    radius: 2,
    message() {
        console.log('hello')
    },
};

// But it isn't the best way to create object and duplicate it if this object has at least 1 method

